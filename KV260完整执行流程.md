# KV260完整执行流程

本文记录 KV260 从 SD 卡制作、首次启动验证到 Runtime 初始化的完整生命周期。当前推荐手动流程为：

```text
prepare_kv260_image.sh
→ 手动 sync
→ 安全拔出 SD 卡
→ 插入 KV260
→ 接网线并上电
→ cloud-init status --wait
→ 检查 hostname / IP / route / DNS
→ 回到写卡 PC
→ ./runtime_init_kv260.sh <Board ID>
→ XRT
→ ZOCL
→ FPGA / XMUtil 检查
→ Minimal PYNQ
→ Final check
```

`deploy_kv260.sh` 是把以上步骤串联起来的一键入口，后文会单独说明。无论使用哪种入口，Board ID 映射和 Image Factory / Runtime Factory 的职责边界都不变。

> 警告：写盘会**完整覆盖目标磁盘**。执行前必须用 `lsblk` 确认目标设备，绝不能将系统盘作为目标。

## 最重要：同一个原始镜像，按板号制作不同 SD 卡

脚本调用格式：

```bash
sudo ./prepare_kv260_image.sh <原始镜像> <目标SD卡> <KV260编号>
```

- `<原始镜像>`：所有 KV260 都使用**同一个**原始 `.img` 文件，例如 `./iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img`。
- `<目标SD卡>`：当前要写入的整张 SD 卡设备，例如 `/dev/sdb`。每次换卡都必须重新执行 `lsblk` 确认，设备名不能想当然地沿用。
- `<KV260编号>`：这是第几台 KV260，范围为 `1–20`。

制作第 2 台时，直接使用同一个原始镜像并传入编号 `2`；**不需要先制作第 1 台，也不需要使用第 1 台生成出来的 SD 卡或镜像。** 每张 SD 卡均从同一原始镜像重新写入，只有最后一个编号参数不同。

```bash
# 第 1 台 KV260：kv2601 / 192.168.31.82
sudo ./prepare_kv260_image.sh \
  ./iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img \
  /dev/sdb \
  1
```

## KV260 编号与网络规则

| 项目 | 值 |
| --- | --- |
| KV260 板号 | `N`，范围 `1–20` |
| 主机名 | `kv260N` |
| IPv4 | `192.168.31.(81 + N)/24` |
| 网关 | `192.168.31.1` |
| 默认账户 | `ubuntu` |
| 默认密码 | `ubuntu` |

例如第 1 台为 `kv2601` / `192.168.31.82/24`，第 20 台为 `kv26020` / `192.168.31.101/24`。每张 SD 卡都必须使用不同板号，避免 hostname 与静态 IP 冲突。

## 1. 确认 SD 卡设备

插入 SD 卡后，先检查块设备、容量、文件系统和挂载点：

```bash
lsblk -o NAME,SIZE,TYPE,FSTYPE,LABEL,MOUNTPOINTS,MODEL,SERIAL
```

本次示例中，新卡为 `/dev/sdb`，其分区 `/dev/sdb1` 自动挂载至 `/media/robot/1234-5678`；系统盘为 `/dev/nvme0n1`，不得写入。实际设备名可能不同，必须以本次 `lsblk` 输出为准。

可选：制作前扫描局域网，确认目标板号对应的静态地址未被其他设备占用：

```bash
sudo nmap -sn -PR 192.168.31.0/24
```

扫描只能发现当前在线且响应 ARP 的设备；离线设备或 DHCP 保留地址仍需在路由器侧确认。地址由板号规则确定，不应临时改用其他编号。

## 2. 解压镜像

脚本输入必须是未压缩的 `.img` 文件。保留原始压缩包并解压：

```bash
xz -dk --verbose \
  iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img.xz
```

确认镜像大小，并确认其小于 SD 卡容量：

```bash
stat -c '%n %s bytes' \
  iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img
lsblk -b -dn -o NAME,SIZE /dev/sdb
```

## 3. 卸载自动挂载的 SD 卡分区

写入整盘前必须卸载目标 SD 卡上所有已挂载分区：

```bash
sudo umount /dev/sdb2 2>/dev/null || true
sudo umount /dev/sdb1
```

如果提示 `target is busy`，先找出占用者：

```bash
sudo fuser -vm /media/robot/1234-5678
```

通常是文件管理器窗口正在浏览 SD 卡。关闭该窗口后再执行卸载；不要在不清楚占用原因时使用强制卸载。

## 4. 写入、扩容和配置

从项目根目录执行。以下是第 1 台的示例；第 2、3…20 台均使用同一个原始镜像，只将最后一个参数改为对应板号：

```bash
sudo ./prepare_kv260_image.sh \
  ./iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img \
  /dev/sdb \
  1
```

脚本会先显示将生成的 hostname/IP，并要求再次输入目标 SD 卡路径。仅在提示中输入：

```text
/dev/sdb
```

脚本依次执行以下操作：

1. 用镜像完整覆盖 `/dev/sdb`。
2. 将第 2 分区扩展到 SD 卡剩余空间，并以 `e2fsck` 和 `resize2fs` 扩展 ext4 rootfs。
3. 挂载 boot 与 rootfs 分区，写入 NoCloud 网络配置。
4. 根据板号配置静态 IP `192.168.31.(81 + N)/24`、默认网关 `192.168.31.1`、主机名 `kv260N`。
5. 将镜像原用户 `kv` 重命名为 `ubuntu`（如果存在），设置密码 `ubuntu`，并配置免密码 sudo。
6. 启用 SSH 密码登录；禁止 root SSH 登录。
7. 配置 TUNA Ubuntu/PyPI 镜像、Xilinx Kria PPA 和首次启动时的 cloud-init 操作。

## 5. 等待与进度检查

写入过程不可拔卡、不可关机。脚本使用 `dd ... conv=fsync`，部分 SD 卡或读卡器可能明显较慢。

若需检查当前写入进度：

```bash
pgrep -af 'prepare_kv260_image|dd if=.*of=/dev/sdb'
sudo cat /proc/<dd_PID>/io
```

其中 `write_bytes` 持续增长表示写入仍在推进。镜像约为 4.12 GiB；在镜像未写完之前，`lsblk` 仍可能显示旧分区表或旧 exFAT 文件系统，这是正常现象。

## 6. 完成后校验

脚本成功时会输出 `KV260 Deployment Complete`、Board ID、Hostname、IP、Target 和用户信息。然后检查分区和 rootfs：

```bash
lsblk -f /dev/sdb
sudo fdisk -l /dev/sdb
```

预期为 boot 分区（第 1 分区）和扩展到接近整张卡容量的 ext4 rootfs（第 2 分区）。随后按下一章执行手动 `sync`、安全拔卡和首次启动验证。以板号 1 为例，设备启动后可尝试：

```bash
ssh ubuntu@192.168.31.82
```

首次登录密码为 `ubuntu`，建议首次登录后立即修改密码。

# SD 卡写入后的 KV260 首次启动与验证

本章补充 SD 卡完成写入后的启动、连接和环境验证流程。脚本第三个参数表示 **KV260 板号**，而不是 IP 最后一段。

## 1. 插卡与启动

确认写卡脚本已正常输出完成信息后，在写卡 PC 上执行：

```bash
sync
```

待命令返回后安全取出 SD 卡，将其插入对应 KV260。把 KV260 接入与写卡 PC 相同的 `192.168.31.0/24` 局域网，再接通电源并等待系统启动。启动时间会随 SD 卡、外设和首次 cloud-init 工作而变化，不使用固定时间作为判断依据。

## 2. 根据板号确定预期 hostname/IP

规则如下：

```text
hostname = kv260N
IPv4 最后一段 = 81 + N
IPv4 = 192.168.31.(81 + N)
```

`kv2601` / `192.168.31.82` 是已实际运行的第 1 台板，作为此规则的基准。

| 板号 | hostname | IPv4 |
| ---: | --- | --- |
| 1 | `kv2601` | `192.168.31.82` |
| 2 | `kv2602` | `192.168.31.83` |
| 3 | `kv2603` | `192.168.31.84` |
| 4 | `kv2604` | `192.168.31.85` |
| 5 | `kv2605` | `192.168.31.86` |
| 6 | `kv2606` | `192.168.31.87` |
| 7 | `kv2607` | `192.168.31.88` |
| 8 | `kv2608` | `192.168.31.89` |
| 9 | `kv2609` | `192.168.31.90` |
| 10 | `kv26010` | `192.168.31.91` |
| 11 | `kv26011` | `192.168.31.92` |
| 12 | `kv26012` | `192.168.31.93` |
| 13 | `kv26013` | `192.168.31.94` |
| 14 | `kv26014` | `192.168.31.95` |
| 15 | `kv26015` | `192.168.31.96` |
| 16 | `kv26016` | `192.168.31.97` |
| 17 | `kv26017` | `192.168.31.98` |
| 18 | `kv26018` | `192.168.31.99` |
| 19 | `kv26019` | `192.168.31.100` |
| 20 | `kv26020` | `192.168.31.101` |

例如，制作第 5 台时执行：

```bash
sudo ./prepare_kv260_image.sh kv260.img /dev/sdb 5
```

预期 hostname 为 `kv2605`，IP 为 `192.168.31.86/24`，默认网关始终为 `192.168.31.1`。

> 每一张 SD 卡必须使用不同的 KV260 编号。若多张卡都使用同一编号，会造成 hostname 和静态 IP 冲突。

## 3. Ping 测试

以第 1 台为例，在 PC 上执行：

```bash
ping 192.168.31.82
```

出现 `64 bytes from 192.168.31.82` 表示该地址网络可达，但还不能百分百确认设备就是目标 KV260；还应通过 SSH 和 hostname 继续确认。

若出现 `Destination Host Unreachable`，检查 KV260 是否已开机、网线和交换机连接、PC 与 KV260 是否在同一网段、IP 是否冲突，以及静态 IP 是否实际生效。

## 4. SSH 登录

第 1 台的登录命令为：

```bash
ssh ubuntu@192.168.31.82
```

第 2 台为 `ssh ubuntu@192.168.31.83`，其他板按表中地址替换。首次连接若询问是否继续连接，确认指纹来源正确后输入 `yes`，再输入当前烧卡配置中为 `ubuntu` 设置的密码。

## 5. 确认主机和网络

登录后先等待 cloud-init 完成，再检查主机、网络、路由和 DNS：

```bash
hostname
hostname -I
ip addr
ip route
cloud-init status --wait
ping -c 3 192.168.31.1
ping -c 3 223.5.5.5
ping -c 3 mirrors.tuna.tsinghua.edu.cn
```

`hostname` 用于确认板号；`hostname -I` 快速显示地址；`ip addr` 显示网卡和地址；`ip route` 应显示经 `192.168.31.1` 的默认路由。网关 `192.168.31.1` 可达表示基础局域网正常；`223.5.5.5` 可达表示公网 IP 路由正常；`mirrors.tuna.tsinghua.edu.cn` 能解析并 ping 通表示 DNS 正常。正确命令是 `ip addr`，不是 `ip-addr`。

## 6. 第一台 kv2601 的实测环境

下面是第 1 台 `kv2601` 的实测结果，其他板的 hostname/IP 应按编号规则变化：

```text
hostname:          kv2601
IPv4:              192.168.31.82
network interface: eth0
gateway:           192.168.31.1
系统:              Ubuntu 24.04.4 LTS
Kernel:            6.8.0-1035-xilinx
architecture:      aarch64
SSH user:          ubuntu
```

其关键网络输出为：

```text
eth0: inet 192.168.31.82/24
default via 192.168.31.1 dev eth0
192.168.31.0/24 dev eth0
```

实际可通过 `ssh ubuntu@192.168.31.82` 登录，并显示 Ubuntu 24.04.4 LTS、`6.8.0-1035-xilinx` 和 `aarch64`。

## 7. 确认 Ubuntu/Xilinx 系统

执行：

```bash
uname -a
cat /etc/os-release
uname -m
```

这可确认 Ubuntu、Xilinx 内核和 ARM64 架构是否正常。前面执行的 `cloud-init status --wait` 最终应显示 `status: done`。若不是，先查看：

```bash
cloud-init status --long
journalctl -u cloud-init
journalctl -u cloud-final
```

不要在未检查日志前直接重新烧卡。

## 8. 可选的 FPGA 平台基础检查

以下仅用于检查、记录和确认环境，不会安装 PYNQ 或修改系统：

```bash
ls -l /sys/class/fpga_manager/
grep -i cma /proc/meminfo
dmesg | grep -i cma
lsmod | grep -Ei 'zocl|xrt'
dpkg -l | grep -Ei 'xrt|zocl|xilinx'
which xrt-smi || true
xrt-smi examine 2>&1 || true
python3 --version
pip3 --version
```

这些命令是可选诊断，不是 Runtime 初始化前的必选步骤。若尚未执行 Runtime，XRT 或 ZOCL 检查失败可能只是组件尚未安装；标准下一步仍是回到写卡 PC 执行 `runtime_init_kv260.sh <Board ID>`。

## 9. 常见问题

| 现象 | 排查方向 |
| --- | --- |
| `ping: Destination Host Unreachable` | 检查供电、网线、交换机、同网段、IP 冲突与静态配置。 |
| `ssh: No route to host` | 检查 PC 路由、二层连接和目标 IP；先执行 ping。 |
| `ssh: Connection refused` | 目标网络已可达但 SSH 服务未监听；登录串口或检查 `cloud-init status --wait`、`systemctl status ssh`。 |
| `ip-addr: command not found` | 使用正确命令 `ip addr`。 |
| hostname 不符合 `kv260N` | 运行 `hostname`、`cat /etc/hostname`，并检查写卡时的板号。 |
| IP 不符合 `192.168.31.(81+N)` | 运行 `hostname -I`、`ip addr`、`ip route`，检查 `/etc/netplan/`。 |
| 两块 KV260 IP 冲突 | 关闭其中一块或断网后，确认每张 SD 卡使用不同板号再重新制作。 |
| cloud-init 一直不是 `done` | 查看 `cloud-init status --long` 与 cloud-init/cloud-final 日志；不直接修改配置。 |

必要时可进一步检查 NetworkManager，但不要未经确认直接修改：

```bash
nmcli connection show
nmcli device show eth0
```

# 多 KV260 批量部署流程

本项目面向 20 台 KV260，所有编号都遵循同一规则：

```text
Board ID:  1 ~ 20
Hostname:  kv260${ID}
IPv4:      192.168.31.(81 + ID)
```

例如：

| Board ID | hostname | IPv4 |
| ---: | --- | --- |
| 1 | `kv2601` | `192.168.31.82` |
| 2 | `kv2602` | `192.168.31.83` |
| 3 | `kv2603` | `192.168.31.84` |
| 20 | `kv26020` | `192.168.31.101` |

每张 SD 卡都从**同一个原始 `.img` 文件**重新制作；不需要先制作编号 1，也绝不能把已制作的某张 SD 卡当作下一张卡的镜像来源。唯一变化的是最后一个 `Board ID` 参数。

```bash
# 制作 kv2601 / 192.168.31.82
sudo ./prepare_kv260_image.sh kv260.img /dev/sdb 1

# 制作 kv2602 / 192.168.31.83
sudo ./prepare_kv260_image.sh kv260.img /dev/sdb 2

# 制作 kv2603 / 192.168.31.84
sudo ./prepare_kv260_image.sh kv260.img /dev/sdb 3
```

每次换卡后都重新执行 `lsblk`。仅当容量、挂载点和物理设备均确认无误后，才把该次显示的整盘设备（例如 `/dev/sdb`）传给脚本。

## Golden Image 推荐流程

```text
Base Image（同一个原始 .img）
          |
          v
prepare_kv260_image.sh <原始镜像> <目标SD卡> <Board ID>
          |
          v
带有独立 hostname/IP 的 KV260 SD 卡
          |
          +-- kv2601 / 192.168.31.82
          +-- kv2602 / 192.168.31.83
          `-- ... kv26020 / 192.168.31.101
```

可将经过验证、用于批量写卡的原始 `.img` 称为 Golden Image。脚本不会从编号 1 生成新的“编号 2 镜像”；它始终从 Golden Image 写入 SD 卡，再把该卡的 hostname、IP、cloud-init 和用户配置个性化。

## SD 制作与 Runtime 配置的边界

`prepare_kv260_image.sh` 是 Image Factory，只负责以下基础部署工作：

- 写入镜像；
- 扩展 rootfs；
- 配置 hostname；
- 配置静态 IP、gateway 和 DNS；
- 创建或迁移 `ubuntu` 用户；
- 配置 SSH；
- 配置 cloud-init 和 NoCloud/netplan。

它**不负责 Runtime 安装**，也不安装 XRT、ZOCL、PYNQ、Vivado、Vitis、FPGA Overlay 或用户应用。首次启动和网络验证通过后，在写卡 PC 上运行 `runtime_init_kv260.sh <Board ID>`；该脚本再通过 SSH 触发 KV260 ARM64 上的 `runtime/install_runtime.sh`。当前仓库不会把 ARM64 Runtime 操作混入 x86 写卡阶段。

# KV260 首次启动、网络与 Runtime 检查

## 1. 静态网络与 cloud-init

脚本同时写入两处 NoCloud seed：

```text
/var/lib/cloud/seed/nocloud/network-config
boot 分区/network-config
```

并写入 `/etc/netplan/99-kv260-static.yaml` 作为持久静态网络配置。该文件使用 NetworkManager、匹配 `eth0`、关闭 DHCP，配置地址 `192.168.31.(81+ID)/24`、默认网关 `192.168.31.1` 和 DNS：

```text
223.5.5.5
192.168.31.1
```

`/etc/cloud/cloud.cfg.d/99-disable-network-config.cfg` 阻止 cloud-init 在首次启动时重新生成或覆盖 netplan；cloud-init 仍会继续处理 hostname、用户、SSH 和包安装。启动后先等待：

```bash
cloud-init status --wait
```

再确认：

```bash
hostname
ip -4 addr show eth0
ip route
resolvectl status eth0 || true
```

## 2. 标准下一步：Runtime 初始化

首次启动、cloud-init 和网络检查全部通过后，退出 KV260 SSH 会话并回到写卡 PC 的项目根目录。以第 2 台为例，执行：

```bash
./runtime_init_kv260.sh 2
```

脚本根据 Board ID 自动计算：

```text
hostname: kv2602
SSH:      ubuntu@192.168.31.83
```

通用 SSH 目标为：

```text
ubuntu@192.168.31.(81 + Board ID)
```

`runtime_init_kv260.sh` 运行在写卡 PC 上。它会核对远端 hostname，上传仓库中的 `runtime/` 和 `scripts/`，然后在 KV260 上执行：

```text
runtime/install_runtime.sh
```

`install_runtime.sh` 的实际 Runtime 流程为：

```text
[1/5] XRT package
[2/5] ZOCL
[3/5] XRT, ZOCL and FPGA/XMUtil checks
[4/5] Minimal PYNQ Runtime
[5/5] Final node report
```

安装脚本自身会执行 XRT、ZOCL、FPGA/XMUtil 检查和最终节点报告，不需要在它之前手动运行 `scripts/kv260_check.sh`。

## 3. 可选诊断：手动检查脚本

`scripts/kv260_check.sh` 不是 Runtime 前的必选步骤，仅用于需要单独复查或定位故障时的可选诊断。`runtime_init_kv260.sh` 会自动上传 `scripts/`；如需手动使用，也可以从写卡 PC 单独复制：

```bash
scp scripts/kv260_check.sh ubuntu@192.168.31.83:/home/ubuntu/
ssh ubuntu@192.168.31.83 'sudo /home/ubuntu/kv260_check.sh'
```

将地址替换为对应 Board ID 的地址。也可以在已同步的项目目录中直接运行 `sudo ./scripts/kv260_check.sh`。

脚本输出 `KV260 Check Report`，检查：

- hostname 是否为 `kv2601` 到 `kv26020`，且 IP 是否符合编号映射；
- Ubuntu、Xilinx kernel、`aarch64` 架构与 CMA；
- `/sys/class/fpga_manager/`；
- `xrt-smi examine`；
- `xmutil listapps`。

检查脚本不安装软件、不加载 Overlay、不修改网络或 FPGA。若 XRT、XMUtil 尚未安装或不可用，对应项会显示 `FAIL` 并给出原因。正常部署应直接运行 `runtime_init_kv260.sh <Board ID>`，由 `install_runtime.sh` 完成安装、检查和最终报告。

## 4. 推荐手动部署生命周期

部署一台或批量 20 台 KV260 时，按以下顺序执行：

1. 确认并准备同一个 Golden/Base Image。
2. 使用 `prepare_kv260_image.sh` 制作每张 SD 卡，并为每张卡指定不同 Board ID。
3. 在写卡 PC 上手动执行 `sync`，等待返回后安全拔出 SD 卡。
4. 将 SD 卡插入 KV260，连接网线并上电。
5. SSH 登录 KV260，等待 `cloud-init status --wait` 完成。
6. 检查 hostname、IP、默认路由、网关、公网 IP 路由和 DNS。
7. 退出 SSH，回到写卡 PC，执行 `./runtime_init_kv260.sh <Board ID>`。
8. 等待 XRT、ZOCL、FPGA/XMUtil、Minimal PYNQ 和 Final node report 全部通过。
9. 对其他板重复相同流程；每台只改变 Board ID，并在换卡后重新用 `lsblk` 确认 SD 设备。

# KV260一键部署流程

本项目保留两种入口，二者职责明确：

- **推荐手动流程**：运行 `prepare_kv260_image.sh`，手动 `sync` 并插卡上电，完成首次启动网络检查后，再在写卡 PC 运行 `runtime_init_kv260.sh <Board ID>`。
- **一键流程**：运行 `deploy_kv260.sh`，由脚本调用 `prepare_kv260_image.sh`、等待目标 IP 上线并调用 `runtime_init_kv260.sh`。

`deploy_kv260.sh` 是写卡 PC 上的一键入口。用户只需提供 KV260 编号、当前确认过的 SD 设备和同一个原始镜像：

```bash
sudo ./deploy_kv260.sh <KV260编号> <目标SD卡> <原始镜像>
```

它不复制 `prepare_kv260_image.sh` 的写卡代码，而是依次调用 Image Factory、上线等待和 Runtime Factory：

```text
deploy_kv260.sh
      |
      +-- SD 制作：prepare_kv260_image.sh
      |       写卡、扩容、hostname、IP、cloud-init、用户配置
      |
      +-- 等待 KV260 上线：最多 30 分钟 ping 检测
      |
      +-- runtime_init_kv260.sh <Board ID>
              计算 hostname/IP、上传 Runtime、建立 SSH
              |
              `-- runtime/install_runtime.sh（仅在 KV260 ARM64 上执行）
              XRT、ZOCL、FPGA/XMUtil、Minimal PYNQ
```

当前部署相关目录结构：

```text
.
├── deploy_kv260.sh
├── prepare_kv260_image.sh
├── runtime_init_kv260.sh
├── runtime/
│   ├── install_runtime.sh
│   ├── install_zocl.sh
│   └── install_pynq.sh
├── scripts/
│   ├── check_xrt.sh
│   ├── check_zocl.sh
│   ├── check_fpga.sh
│   └── kv260_check.sh
├── logs/
└── KV260完整执行流程.md
```

## 部署第 2 台示例

在写卡 PC 的项目根目录执行：

```bash
sudo ./deploy_kv260.sh \
  2 \
  /dev/sdb \
  ./iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img
```

脚本会显示以下部署摘要并要求输入 `DEPLOY`：

```text
Board: kv2602
IP:    192.168.31.83
SD:    /dev/sdb
Image: <原始镜像>
```

之后 `prepare_kv260_image.sh` 仍会要求输入目标设备路径和 `WRITE`，这是有意保留的 SD 覆盖保护。写卡结束后，按提示安全取卡、插入 `kv2602` 并上电；脚本会自动等待 `192.168.31.83` 的 ping 响应，然后调用 `runtime_init_kv260.sh 2`，通过 SSH 上传并运行 Runtime Factory。

首次 SSH 连接使用项目内的 `logs/known_hosts`，并采用 `StrictHostKeyChecking=accept-new`：新主机会被记录，已变更的主机密钥不会被静默接受。同一次初始化会复用 SSH 连接，避免每个步骤重复认证。使用密码认证时，在 SSH 提示处输入当前 `ubuntu` 密码；已配置 SSH key 时无需输入密码。

如果 SD 卡已经制作完成且 KV260 已经启动，也可跳过写卡流程，单独执行：

```bash
./runtime_init_kv260.sh 2
```

该命令自动计算 `kv2602` 和 `192.168.31.83`，不会写入任何 SD 卡。

## Runtime Factory

`runtime_init_kv260.sh` 运行在写卡 PC 上；它上传文件后调用的 `runtime/install_runtime.sh` 及各安装/检查脚本只在已启动的 KV260 ARM64 系统中运行。相关目录如下：

```text
runtime/
├── install_runtime.sh  # 总入口：等待 cloud-init，再按顺序执行各步骤
├── install_zocl.sh     # 检查并加载与当前 kernel 匹配的 zocl
└── install_pynq.sh     # /opt/kv260-pynq 中的 Minimal PYNQ venv

scripts/
├── check_xrt.sh        # xrt-smi examine 和设备可见性
├── check_zocl.sh       # 当前 kernel 模块、lsmod 和 renderD128
├── check_fpga.sh       # FPGA Manager、xmutil listapps/examine
└── kv260_check.sh      # hostname、网络、系统、CMA 和最终节点报告
```

若 `xrt-smi` 不存在，Runtime Factory 默认安装 `xrt` 包；可通过 `XRT_PACKAGE=<包名>` 指定本地 Kria 软件源中的替代包。若 zocl 不存在，脚本只尝试安装精确匹配当前 `uname -r` 的 `linux-modules-extra-<kernel>`；没有匹配包时会停止，并要求显式传入 `ZOCL_PACKAGE=<匹配包名>`。这避免在 ARM64 KV260 上安装错误内核版本的驱动。

Minimal PYNQ 安装在 `/opt/kv260-pynq` 虚拟环境中，不安装 Jupyter、Notebook、DPU 示例或完整开发环境。安装结束会验证 `Overlay`、`MMIO` 和 `allocate` 可导入；DMA 的实际可用性还取决于随后部署的匹配 `.bit`、`.hwh`、设备树和目标驱动。自定义 Overlay 与用户应用仍属于后续应用部署阶段。

Runtime Factory 会先确认 SSH 返回的 hostname 与 Board ID 一致，并确认目标为 `aarch64`、正在运行 Xilinx kernel。随后按照前文列出的 `[1/5]` 至 `[5/5]` 流程执行：

- `[1/5]` 检查或安装 XRT。
- `[2/5]` 查找当前 `uname -r` 对应的 zocl，执行 `depmod`、`modprobe zocl`，并验证 `/dev/dri/renderD128`。
- `[3/5]` 执行 `xrt-smi examine`、ZOCL 检查、FPGA Manager、`xmutil listapps` 和 `xmutil examine`。
- `[4/5]` 安装并验证 Minimal PYNQ Runtime。
- `[5/5]` 执行最终 `KV260 Check Report`。

任一步失败都会停止，不会把尚未通过的 XRT、ZOCL、PYNQ 或 FPGA 状态误报为 `OK`。

全部通过后，一键入口会输出：

```text
KV260 Deployment Complete
Board: kv2602
IP: 192.168.31.83
XRT: OK
ZOCL: OK
PYNQ: OK
FPGA: OK
```

## 日志与故障定位

每次一键部署都追加本机日志：

```text
logs/kv2601.log
logs/kv2602.log
...
logs/known_hosts
```

日志记录写卡开始时间、上线等待、SSH Runtime 输出及完成状态。Runtime 安装失败时，保留该日志并修复目标 KV260 的网络、软件源或内核包问题；不需要重新设计 Golden Image 或改变 Board ID 规则。

## 安全的 20 台批量部署

每次制作必须重新确认实际 SD 设备。不要把未经核对的固定 `/dev/sdX` 放进无人值守循环，因为拔插后设备名可能变化。以下循环要求操作者每台手动输入本次确认的整盘设备：

```bash
for i in {1..20}; do
  read -r -p "kv260${i} 的目标 SD 设备（先运行 lsblk 确认）: " disk
  sudo ./deploy_kv260.sh "$i" "$disk" \
    ./iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img
done
```

每轮的映射恒为：

```text
kv260${i}
192.168.31.(81 + i)
```

因此一张 Golden Image 加一个不同的 Board ID，即可形成一台可 SSH 管理、后续可统一部署 FPGA 应用的独立 KV260 节点。
