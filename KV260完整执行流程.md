# KV260 完整执行流程

本文是操作手册：从 Ubuntu 24.04 Noble 镜像制作 SD 卡，到目标 KV260 完成 XRT/ZOCL/Minimal PYNQ Runtime。系统架构、API 和调度模型见 `KV260_PYNQ_Framework.md`。

> 写盘会完整覆盖目标设备。每次插入或更换 SD 卡后必须重新执行 `lsblk`，不能沿用上一次的 `/dev/sdX` 判断。

## 1. 当前实测基线

当前测试板：

```text
hostname:       kv2602
IPv4:           192.168.31.83/24
gateway:        192.168.31.1
DNS:            223.5.5.5, 192.168.31.1
OS:             Ubuntu 24.04 Noble
architecture:   aarch64
kernel:         6.8.0-1035-xilinx（重启后）
FPGA Manager:   operating
XRT:            2.18.0-0ubuntu1
xrt-dkms:       2.18.0-0ubuntu1
ZOCL module:    /lib/modules/6.8.0-1035-xilinx/updates/dkms/zocl.ko.zst
```

`xrt-dkms=2.18.0-0ubuntu1` 已实测能为 `6.8.0-1015-xilinx` 和 `6.8.0-1035-xilinx` 构建并加载 ZOCL。

## 2. 工厂边界和推荐流程

```text
写卡 PC
  prepare_kv260_image.sh
          ↓
  安全插卡、KV260 上电、cloud-init
          ↓
  runtime_init_kv260.sh <Board ID>
          ↓ SSH
KV260
  install_runtime.sh
    System → XRT → xrt-dkms/ZOCL
       ├── 新 kernel：REBOOT_REQUIRED
       │        ↓ PC 自动 reboot/wait/resume
       └────────┘
    PYNQ → DT/runtime → allocate test → final report
```

- Image Factory：`prepare_kv260_image.sh`，只处理 SD、系统身份和网络。
- Runtime Factory launcher：`runtime_init_kv260.sh`，在 PC 上验证目标、上传文件并处理重启续跑。
- Runtime Factory：`runtime/install_runtime.sh`，只在已启动的 aarch64 KV260 上运行。
- 一键入口：`deploy_kv260.sh`，串联写卡、等待上线和 Runtime Factory。

## 3. 制作 SD 卡

### 3.1 确认设备与镜像

```bash
lsblk -o NAME,SIZE,TYPE,FSTYPE,LABEL,MOUNTPOINTS,MODEL,SERIAL
stat -c '%n %s bytes' ./kv260.img
lsblk -b -dn -o NAME,SIZE /dev/sdb
```

如目标卡分区被自动挂载，先卸载：

```bash
sudo umount /dev/sdb2 2>/dev/null || true
sudo umount /dev/sdb1 2>/dev/null || true
```

只有 `.img.xz` 时保留压缩包并解压：

```bash
xz -dk --verbose ./kv260.img.xz
```

### 3.2 运行 Image Factory

```bash
sudo ./prepare_kv260_image.sh <原始镜像> <目标整盘设备> <Board ID>
```

制作第 2 台：

```bash
sudo ./prepare_kv260_image.sh \
  ./iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img \
  /dev/sdb \
  2
```

确认摘要必须是：

```text
Board ID: 2
Hostname: kv2602
IP: 192.168.31.83
Target: /dev/sdb
```

脚本需要两次覆盖确认，然后写镜像、扩展 ext4 rootfs，并配置：

- `hostname=kv260N`；
- `192.168.31.(81+N)/24`；
- gateway `192.168.31.1`；
- DNS `223.5.5.5`、`192.168.31.1`；
- `ubuntu` 用户、SSH、NoCloud/netplan 和首次启动任务。

完成后执行并等待返回：

```bash
sync
```

然后安全拔卡、插入对应 KV260、连接网络并上电。

## 4. 首次启动检查

第 2 台：

```bash
ping 192.168.31.83
ssh ubuntu@192.168.31.83
```

在 KV260 内执行：

```bash
cloud-init status --wait
hostname
hostname -I
ip addr show eth0
ip route
resolvectl status eth0
uname -r
uname -m
cat /sys/class/fpga_manager/fpga0/state
grep -E '^Cma(Total|Free):' /proc/meminfo
```

预期 `cloud-init=done`、hostname/IP 与 Board ID 一致、`aarch64`、Xilinx kernel、FPGA Manager 为 `operating`。cloud-init 返回码 `2` 表示带 recoverable warnings 完成，Runtime 会警告后继续；其他非零状态会停止。

检查完退出目标：

```bash
exit
```

## 5. 运行 Runtime Factory

回到写卡 PC 的仓库根目录：

```bash
./runtime_init_kv260.sh 2
```

脚本会核对远端 hostname、上传 `runtime/` 和 `scripts/`，然后执行：

```text
[preflight] System and FPGA Manager
[1/7] XRT userspace
[2/7] XRT-matched ZOCL DKMS driver
[3/7] XRT-matched Python 3.12 pyxrt binding
[4/7] Minimal PYNQ 3.1.2 packages and runtime assets
[5/7] PYNQ device tree and boot services
[6/7] Minimal PYNQ functional validation
[7/7] Final diagnostics and worker runtime report
```

密码认证时可能在首次连接和自动重启后各要求一次密码。日志追加到 `logs/kv260N.log`。

## 6. XRT 与 ZOCL 的正确安装流程

### 6.1 XRT userspace

`runtime/install_xrt.sh`：

1. 幂等确认 `ppa:ubuntu-xilinx/sdk`；不存在时用 `add-apt-repository` 为当前 Noble 添加。
2. 只接受 SDK PPA 中的 XRT version。
3. 已安装 XRT 时保留其准确 Debian version；未安装时选择 SDK 提供的版本。
4. 安装后用 `dpkg-query` 复核并确认 `xrt-smi` 命令存在；PYNQ 阶段还会确认 XRT 提供的 `xclbinutil`，用于从 HWH 构造嵌入式 memory topology。

可以显式固定版本：

```bash
sudo XRT_VERSION=2.18.0-0ubuntu1 ./runtime/install_xrt.sh
```

### 6.2 ZOCL / xrt-dkms

`runtime/install_zocl.sh` 的顺序是：

1. 读取 `kernel=$(uname -r)`。
2. 检查 `/lib/modules/$kernel/build`；缺失时安装 `linux-headers-$kernel`，失败即停止。
3. 读取 `dpkg-query -W -f='${Version}' xrt`。
4. 确认 Noble 的 `ppa:ubuntu-xilinx/sdk` 并更新 apt metadata。
5. 枚举 SDK PPA 的 `xrt-dkms` version，要求存在与 XRT 完全相同的值。
6. 不匹配时输出 XRT version、candidate 和 SDK available versions，并以 `XRT / ZOCL version mismatch` 停止。
7. 安装精确版本：`apt-get install -y xrt-dkms=$XRT_VERSION`。
8. 复核 `dkms status` 中当前 kernel/aarch64 的 `xrt/...: installed`。
9. 执行 `depmod`、`modprobe zocl`。
10. 检查当前 kernel 的 `*zocl*.ko*` 和 `/proc/modules` 中的 zocl。

绝不再尝试：

```text
linux-modules-extra-$(uname -r)
```

也绝不接受 `xrt=2.18` 与 Noble universe 的旧 `xrt-dkms=2.8...` 混装。

手动复查：

```bash
dpkg-query -W -f='${Package} ${Version}\n' xrt xrt-dkms
dkms status
find "/lib/modules/$(uname -r)" -name '*zocl*.ko*'
lsmod | grep '^zocl'
```

满足“module 存在且 loaded”即：

```text
ZOCL Driver Installation: OK
```

## 7. Kernel 更新与自动 reboot/resume

如果当前运行 `6.8.0-1015-xilinx`，但 `/lib/modules` 已有更新的 `6.8.0-1035-xilinx`，ZOCL 安装脚本会先确认新 kernel headers 和 DKMS module，然后输出：

```text
REBOOT_REQUIRED
Running kernel: 6.8.0-1015-xilinx
Installed newer Xilinx kernel: 6.8.0-1035-xilinx
```

并返回退出码 `75`。这时 Runtime 不能继续误报成功。

通过 `runtime_init_kv260.sh` 或 `deploy_kv260.sh` 启动时，PC 端会：

1. 请求目标 `systemctl reboot`；
2. 等待 SSH 端口关闭；
3. 最长等待 30 分钟恢复；
4. 再次核对 hostname；
5. 重新上传脚本并继续 Runtime Stage 2。

直接在板上运行 `install_runtime.sh` 时，不会自行重启；用户应看到 `REBOOT_REQUIRED` 后手动 reboot，再重复执行。

## 8. Minimal Kria-PYNQ 安装结构

`runtime/install_pynq.sh` 针对 Noble 实现以下结构：

```text
/opt/kv260-pynq/                  Python 3.12 venv（system site packages）
└── share/kv260-runtime/
    ├── pynq.dts / pynq.dtbo
    ├── insert_dtbo.py
    ├── clear_pl_state.py
    └── validate_runtime.py

/etc/profile.d/kv260-pynq.sh
/etc/xocl.txt
/etc/systemd/system/kv260-pynq-dt.service
/etc/systemd/system/kv260-pynq-clear-pl-state.service
```

固定默认版本：

```text
PYNQ=3.1.2
pynqmetadata=0.1.9
pynqutils=0.1.2
numpy=1.26.4
setuptools=80.0.0
grpcio=1.64.0
grpcio-tools=1.64.0
```

`runtime/install_pyxrt.sh` 先读取已安装 `xrt` 的精确 Debian 版本，再从已启用 `deb-src` 的 `ubuntu-xilinx/sdk` 获取完全相同版本的 `xilinx-runtime` source package。脚本只编译其中的 `src/python/pybind11/src/pyxrt.cpp`，链接系统 XRT 2.18 的 headers 与 `libxrt_coreutil.so`，并安装到 Python 3.12 的 `/usr/local` dist-packages。Noble universe 中同名但无关的旧 Python package 不会安装。

venv 使用 `--system-site-packages`，使 `/opt/kv260-pynq` 能加载上述与 XRT 精确匹配的系统 `pyxrt`。已有 venv 会复用并修正该设置；Python major/minor 不一致会明确停止。PYNQ 安装使用 `PYNQ_REMOTE=1` 作为 `setup.py` 构建开关，仅跳过本项目不需要的 HDMI、DisplayPort、Audio 与 PCam native extensions；运行时仍然使用 KV260 上的 `EmbeddedDevice`，不是 `RemoteDevice`。所有 Python package 均由 root 通过 venv interpreter 安装并执行 `pip check`，不会回退到 `/home/ubuntu/.local`。

脚本编译一个最小 PYNQ DT fragment，同时增加 `compatible = "xlnx,zocl"` 和 `/chosen/pynq_board = "KV260"`。后者是 PYNQ 3.1.2 判定 `ON_TARGET=True` 并选择 `EmbeddedDevice` 的目标板标记。官方旧 DTS 中标为 TODO 的 AFI 片段和固定 `0xA0000000` generic-uio 节点没有复制，避免与用户设计冲突。systemd service 只有在两个 live Device Tree 标记都完整时才判定已安装；只会安全修复自己管理的 `overlays/pynq`，不会删除其他 overlay。

PL state service 在每次启动时清除 PYNQ 的 stale global state，再由未来 Worker 加载实际 Overlay。它不依赖 Jupyter。

### 8.1 Allocator 验证

PYNQ 3.1.2 的 `allocate()` 走 XRT BO backend，不再以旧 `libcma.so` 作为当前调用路径。Runtime 会真实执行：

```text
allocate → 写入 → flush → invalidate → 读回 → freebuffer
```

这不是 import-only 检查。没有 design 文件时，用显式 PS DDR target 验证 allocator backend；实际 Overlay 加载后，PYNQ 从 HWH memory topology 自动选择默认 PS DDR。

## 9. Overlay 硬件测试

应用文件默认部署到：

```text
/opt/fpga/design.bit
/opt/fpga/design.hwh
```

两个文件都存在时，Runtime 会执行：

```python
overlay = Overlay("/opt/fpga/design.bit", download=True)
print(overlay.ip_dict)
```

并报告 Overlay Load、HWH Parse 和 AXI DMA discovery。只有一个文件存在会失败，防止 bit/hwh 混配。

两个文件都不存在时不会伪造结果：

```text
PYNQ Core Runtime: OK
Overlay Hardware Test: NOT RUN (design files unavailable)
```

真实 DMA send/receive 还需要与当前 Accelerator 协议匹配的测试向量；没有设计和协议时不能宣称 DMA 计算已经通过。

## 10. `renderD128` 与 `xrt-smi 0 devices`

必须区分：

```text
ZOCL Driver
  xrt-dkms installed
  zocl.ko exists for running kernel
  zocl module loaded

XRT Accelerator Platform
  live DT xlnx,zocl node
  DRM render node
  xrt-smi device enumeration
```

`k26-starter-kits` base firmware 没有 `xlnx,zocl` 时，即使 DKMS 正确，`/dev/dri/renderD128` 也可能不存在，`xrt-smi examine` 也可能显示 0 device。因此它们不是 ZOCL Driver 安装判据。

最终报告把固定的 `renderD128` 路径和 `xrt-smi` device count 作为 `DIAGNOSTIC`。Minimal PYNQ 会加载自己的 runtime DT，并以真实 `allocate()` 是否成功验收，而不是硬编码设备节点名称。

本项目应用路径是：

```text
design.bit + design.hwh + PYNQ Overlay + MMIO/AXI DMA
```

不是：

```text
xclbin + XRT compute scheduler/kernel API
```

XRT/ZOCL 仍为 PYNQ allocator 提供底层能力，但不承担 20 板集群调度。

## 11. 最终成功输出与手动检查

成功报告应包含：

```text
System:                    OK
Xilinx Kernel:             OK
Architecture:              OK
CMA:                       OK
FPGA Manager:              OK
XRT / xrt-dkms:            OK
ZOCL Driver:               OK
pyxrt:                     OK
PYNQ DT/runtime:            OK
PYNQ version:              3.1.2
PYNQ ON_TARGET:            True
Device:                    EmbeddedDevice
allocate:                  OK
Minimal PYNQ:              OK
KV260 PYNQ Worker Runtime: OK
```

诊断项即使显示 `renderD128 absent` 或 `xrt-smi 0 devices`，也不会单独把上述 Runtime 判为失败。

手动复查：

```bash
sudo ./scripts/check_zocl.sh
sudo ./scripts/check_xrt.sh
sudo ./scripts/check_fpga.sh
sudo ./scripts/kv260_check.sh
systemctl status kv260-pynq-dt.service kv260-pynq-clear-pl-state.service
```

## 12. Central Scheduler 与 Worker 的关系

Runtime Factory 只把单板准备成 PYNQ Worker 的运行基础。未来 Worker service 启动时加载一次 `/opt/fpga/design.bit`，然后串行处理 `/predict`；Central Server 维护 `idle/busy/offline/error`，在 20 台板间调度 Job。

本次仓库尚未实现 `worker/` 或 `scheduler/`，最终报告不能把它们标记为 OK。接口和状态机设计见架构文档。

## 13. 一键部署

```bash
sudo ./deploy_kv260.sh \
  2 \
  /dev/sdb \
  ./iot-limerick-kria-classic-server-2404-classic-24.04-x07-20250423.img
```

该入口保留写盘多次确认，然后最多等待 30 分钟上线，并调用 `runtime_init_kv260.sh 2`。它能处理 Runtime 请求的一次 kernel reboot/resume。

## 14. 20 台板地址规则

```text
N = 1 … 20
hostname = kv260N
IP = 192.168.31.(81 + N)
```

| Board ID | Hostname | IP |
| ---: | --- | --- |
| 1 | `kv2601` | `192.168.31.82` |
| 2 | `kv2602` | `192.168.31.83` |
| 3 | `kv2603` | `192.168.31.84` |
| 20 | `kv26020` | `192.168.31.101` |

所有 SD 卡都从同一个原始镜像制作，只改变 Board ID。不同板不能复用同一 ID。

## 15. 故障定位

| 现象 | 处理 |
| --- | --- |
| `linux-headers-<kernel>` 无候选 | 当前 SDK/Kria 软件源没有运行 kernel headers；停止，不构建其他 kernel。 |
| `XRT / ZOCL version mismatch` | 查看输出的 XRT/candidate/available versions，修复软件源或明确版本；不能装旧 universe DKMS。 |
| `REBOOT_REQUIRED` | 用 PC 端 launcher 自动续跑，或手动 reboot 后重复 Runtime。 |
| zocl module 有但 `renderD128` 无 | 先看 ZOCL Driver 是否 OK；基础 DT 没有 platform node 不等于 DKMS 失败。 |
| PYNQ DT service 失败 | 查看 `journalctl -u kv260-pynq-dt.service` 和 live Device Tree；不要反复手工创建 configfs 目录。 |
| `allocate` 失败 | 检查 CMA、`pyxrt`、PYNQ DT、XRT/ZOCL package match 和 service 日志；这是功能失败，不能忽略。 |
| 只有 `.bit` 或 `.hwh` | 部署同一次 Vivado build 的完整匹配文件。 |
| cloud-init 非 0/2 | 查看 `cloud-init status --long`、`journalctl -u cloud-init -u cloud-final`。 |
| Runtime 中止 | 从 `logs/kv260N.log` 的首个 FAIL/exception 定位；不应重新烧卡掩盖 Runtime 问题。 |

## 16. 官方来源与兼容性说明

参考 [Xilinx/Kria-PYNQ](https://github.com/Xilinx/Kria-PYNQ) 的 [install.sh](https://github.com/Xilinx/Kria-PYNQ/blob/main/install.sh) 和 [Xilinx/PYNQ](https://github.com/Xilinx/PYNQ)。官方 Kria 脚本限定 Ubuntu 22.04、Python 3.10、PYNQ 3.0.1 和 Jammy repository，因此本项目不执行或整份复制它。

保留并适配：PYNQ venv、pynqmetadata/pynqutils、BOARD/XRT 环境、`/etc/xocl.txt` 兼容项、PYNQ DT、PL state 清理和 allocator 功能验证。

明确不安装：Jupyter/JupyterLab、Notebook、HelloWorld、Base Overlay、Composable Pipeline、DPU-PYNQ、peripherals/examples、摄像头/OpenCV Demo、Notebook test stack 和 MicroBlaze 工具链。
